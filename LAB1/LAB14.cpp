#include <iostream>
#include <cmath> //we need this library so sqrt works
#define ROOT(x) sqrt(x)

using namespace std;
int main(){
    double i;
    cout <<"give number to be squared";
    cin >> i;
    cout << ROOT(i);
}